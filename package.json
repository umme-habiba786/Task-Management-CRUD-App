{
  "name": "my-node-app",
  "version": "1.0.0",
  "description": "Sample Node.js app for Docker testing",
  "main": "src/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "mkdir -p dist && cp src/index.js dist/index.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}

